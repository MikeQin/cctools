---
description: Start backend service
---
**START BACKEND**:

⚠️ **CUSTOMIZE THIS COMMAND** - Update with your actual startup script

Example:

```bash
# Replace with your startup script
./start-backend.sh

# Or use your framework's command:
# FastAPI: uvicorn main:app --host 0.0.0.0 --port 8080
# Flask: python app.py
# Express: npm start
# Django: python manage.py runserver 0.0.0.0:8080
```

Verify it's running:
```bash
/status
```
